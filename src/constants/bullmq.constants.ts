export enum BULLMQ_QUEUE {
  IMAGE_DESCRIBE = "image-describe",
  IMAGE_COMPARE = "image-compare",
  IMAGE_OCR = "image-ocr",
}

export enum BULLMQ_JOB {
  DESCRIBE_IMAGE = "describe-image",
  COMPARE_IMAGES = "compare-images",
  OCR_IMAGE = "ocr-image",
}
